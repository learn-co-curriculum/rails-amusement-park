<!DOCTYPE html>
<html>
  <%= form_for(:user, url: signin_path) do |f| %>
    <%= f.select :name, @users.collect { |p| [ p.name, p.id ] }%><br />
    Password:<%= f.password_field :password %><br />
    <%= f.submit "Sign In"%>
  <% end %>
</html>
